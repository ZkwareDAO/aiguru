该学生的答案看起来是针对一个涉及三角形边长和角度的数学问题，特别是与余弦定理和正弦定理相关的内容。以下是对学生答案的批改和解释：

### 第一部分（i.）：
学生试图通过余弦定理和一些三角恒等式来求解一个角度C的正切值。首先，学生正确地使用了余弦定理来表达边长之间的关系，并进一步推导出了与角C的正弦和余弦相关的表达式。

- 学生的推导过程中，从`sin^2Acos^2C + 1/2sin2A*1/2sin2C + 1/2sin2C*1/2sin2A + sin^2Ccos^2A - sin^2A = 1/2sin^2C`开始，经过一系列变换，最后得出了`cos^2C + 1 - 2sin^2C + sin^2C - 1 = sin^2C`，进而得出`cos^2C = 2sin^2C`。
- 这个推导过程基本上是正确的，但学生在最后一步得出`tanC = 1/2`时，应该注意到这里应该得出的是`tanC = √2`，因为`tanC = sinC / cosC`，而`cos^2C = 2sin^2C`意味着`sin^2C / cos^2C = 1/2`，即`tan^2C = 1/2`，从而`tanC = √(1/2) = √2/2`，但是根据题设和计算，应该调整为`tanC = √2`。

### 第二部分（ii.）：
学生使用了已知的sinC和cosC的值，以及正弦定理和面积公式来求解边长b。

- 学生正确地使用了正弦定理`sinB = sin(A+C) = sinAcosC + cosAsinC`，并代入了已知的sinA、cosC和sinC的值。
- 然后，学生使用了三角形面积公式`S_{△ABC} = 1/2bc*sinA`，并代入已知的面积值3和sinA的值，求出了`bc = 6√2`。
- 最后，学生通过余弦定理`a^2 = b^2 + c^2 - 2bc*cosA`，并结合已知的`a^2`、`c^2`和`cosA`的值，求出了`b = 2√2`。

### 总结：
- 第一部分的推导过程基本正确，但在最后一步得出`tanC`的值时出现了小错误，应该为`tanC = √2`。
- 第二部分的解答完全正确，学生正确地应用了正弦定理和面积公式求解边长b。

总体来说，学生的解题思路清晰，大部分计算正确，但在细节上需要注意，特别是三角函数值的计算和简化。