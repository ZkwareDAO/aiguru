success, result = process_pdf_merge(
    original_pdf,  # 原始PDF文件
    answer_file,   # 答案文件
    annotation_file,  # 注释文件
    output_dir     # 输出目录
)

if success:
    # 处理成功，result是输出文件路径
    output_path = result
else:
    # 处理失败，result是错误信息
    error_message = result