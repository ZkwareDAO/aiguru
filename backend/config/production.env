# Production Environment Configuration
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# Production security
ALLOWED_HOSTS=yourdomain.com,api.yourdomain.com
CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Production database (set via Railway environment variables)
# DATABASE_URL will be provided by Railway PostgreSQL addon

# Production Redis (set via Railway environment variables)
# REDIS_URL will be provided by Railway Redis addon

# Production file storage
AWS_REGION=us-east-1

# Production email
SMTP_USE_TLS=true